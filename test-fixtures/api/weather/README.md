# Weather API Fixtures

Weather fixtures must cover Google Weather, OpenWeather, and NWS/weather.gov
normalization without exposing provider keys or sensitive load/route data.

Required future scenarios include current conditions, hourly forecasts, daily
forecasts, public alerts, malformed payloads, empty payloads, and classified
provider failures.

