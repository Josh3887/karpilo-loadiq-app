export const verifyApiContractIndexScaffold = {
  status: "scaffold",
  purpose:
    "Future check that docs/api-contracts/README.md provider index rows map to contract files, test files, fixture directories, owners, env vars, and last verified dates.",
  normalCiBehavior: "metadata-only until a test runner or script runner is added",
  liveProviderAccess: false,
} as const;

